
%pip install redis 

--error fix---
!sudo apt update
!sudo apt install redis-server -y
 
!sudo service redis-server start
import redis as red
client = red.Redis(host='localhost', port=6379)
client.ping()
--errorfix---

import redis_server
 
import redis
client = redis.Redis(host = 'localhost', port=6379)
client.ping()

client.set('foo', 'bar')
client.get('foo')


 